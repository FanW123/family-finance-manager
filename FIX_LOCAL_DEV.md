# 🔧 修复本地开发环境

## 问题 1: 缺少 @supabase/supabase-js 依赖

### 解决方案

在项目根目录打开终端，运行：

```bash
cd client
npm install @supabase/supabase-js
```

或者，如果你在项目根目录：

```bash
cd /Users/fanwang/SideProjects/client
npm install @supabase/supabase-js
```

### 验证安装

安装完成后，检查 `client/package.json` 中是否包含：

```json
"dependencies": {
  "@supabase/supabase-js": "^2.39.0",
  ...
}
```

### 重新启动开发服务器

```bash
# 如果开发服务器正在运行，先停止（Ctrl+C）
# 然后重新启动
cd client
npm run dev
```

## 问题 2: 邮箱验证链接问题

### 可能的原因

1. **Supabase 邮箱验证默认启用**：注册后需要点击邮箱中的验证链接
2. **邮箱验证设置**：可能需要在 Supabase 中配置邮箱验证

### 解决方案

#### 选项 A: 禁用邮箱验证（开发测试用）

1. 打开 Supabase Dashboard: https://supabase.com/dashboard/project/xmxvtpuqcnysbvljdohf
2. 进入 **Authentication** → **Providers** → **Email**
3. 找到 **"Confirm email"** 选项
4. **取消勾选** "Enable email confirmations"
5. 点击 **Save**

这样注册后就可以直接登录，不需要验证邮箱。

#### 选项 B: 配置邮箱验证（生产环境推荐）

1. 在 Supabase Dashboard → **Authentication** → **Email Templates**
2. 可以自定义验证邮件模板
3. 确保邮箱服务已配置（默认使用 Supabase 的邮件服务）

### 测试注册流程

1. 清除浏览器缓存或使用无痕模式
2. 访问应用
3. 点击"注册"
4. 输入邮箱和密码
5. 如果禁用了邮箱验证，应该直接登录成功
6. 如果启用了邮箱验证，检查邮箱（包括垃圾邮件文件夹）

## 快速修复命令

```bash
# 1. 安装依赖
cd /Users/fanwang/SideProjects/client
npm install @supabase/supabase-js

# 2. 重新启动开发服务器
npm run dev
```

## 如果还有问题

1. 检查浏览器控制台（F12）的错误信息
2. 确认环境变量已设置（`.env` 文件）
3. 确认 Supabase Authentication 已启用

